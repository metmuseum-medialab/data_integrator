/*
code secrets. copy to secrets.js, modify values, but don't commit.
*/
function secrets(){

	var secrets = { 
		db_name : "",
		port: ,
		prod_port : 
	};

	return secrets;

}

module.exports.secrets = secrets;